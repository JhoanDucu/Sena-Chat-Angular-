import { GruposDirective } from './grupos.directive';

describe('GruposDirective', () => {
  it('should create an instance', () => {
    const directive = new GruposDirective();
    expect(directive).toBeTruthy();
  });
});
